#include "pet.h"                                    // include header file

Pet::Pet(const std::string& species, const std::string& name) : species{species}, name{name} { }   // constructor Pet of class pet. Takes in string reference (species, name) and initializes species to species and name to name
